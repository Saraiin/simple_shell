#ifndef LSHELL_H
#define LSHELL_H
#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

/* for string */
size_t str_len(char *s);
char *str_dup(char *s);

#endif
